"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs = require("fs");
const path = require("path");
// The root of our package, so that we can leverage the scripts in the `package.json`
exports.rootDirectory = path.join(__dirname, "..");
exports.DEFAULT_TEMPLATE_PATH = `${exports.rootDirectory}/_templates`;
// Grab the path of the user's project
exports.pathWhereScriptIsRunning = process.cwd();
// This is where their templates are (at least we assume so)
const theirTemplatePath = exports.pathWhereScriptIsRunning + "/_templates";
const hasTemplates = fs.existsSync(theirTemplatePath);
exports.getTemplateLocation = () => hasTemplates ? theirTemplatePath : exports.DEFAULT_TEMPLATE_PATH;
